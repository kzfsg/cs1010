
function biggie_size(x) {
    return x + 4;
}

function unbiggie_size(x) {
    return x - 4;
}

function is_biggie_size(x) {
    return x > 4 ? true : false;
}

function combo

biggie_size(1);
unbiggie_size(5);
is_biggie_size(5);